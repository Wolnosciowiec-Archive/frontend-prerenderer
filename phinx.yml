paths:
    migrations: %%PHINX_CONFIG_DIR%%/src/Migration
    seeds: %%PHINX_CONFIG_DIR%%/src/Seed

environments:
    default_migration_table: phinxlog
    default_database: production
    production:
        adapter: sqlite
        name: ./data/storage.sqlite3

version_order: creation
